project(':module-chat'){
    dependencies {
        implementation project(':module-common')

    }

    ext {
        set('snippetsDir', file("build/generated-snippets"))
    }

    repositories {
        mavenCentral()
    }
}
